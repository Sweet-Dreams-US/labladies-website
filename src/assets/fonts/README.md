Inter ExtraBold (800), from Google Fonts. SIL Open Font License 1.1 — free to
bundle and redistribute. Used only by the blog's generated share images
(`src/app/(site)/blog/[slug]/opengraph-image.tsx`); the site itself loads Inter
through `next/font`.
